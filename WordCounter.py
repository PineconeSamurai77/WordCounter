package main

import (
    "bufio"
    "fmt"
    "strings"
)

func main() {
    fmt.Println("Enter a string:")
    reader := bufio.NewReaderSize(strings.NewReader("This is a Go example"), 1024)
    input, _ := reader.ReadString( word Counter ....
